# Карта локальных проблем (Ханты-Мансийск)

Веб-приложение для сбора и управления локальными проблемами инфраструктуры через интерактивную карту.  
Реализовано на Django + PostGIS + MapLibre GL.

---

## Архитектура

Проект построен по принципу явного разделения ответственности. Все компоненты изолированы и легко тестируемы.

### Модельный слой
- **`CustomUser`**  
  `email` — основной идентификатор. Поддержка ФИО (включая отчество), роли (`citizen`/`official`), телефона, департамента. Верификация email через `UUID4`-токен.
- **`Issue`**  
  Геометрия хранится как `PointField(srid=4326)`. Инвариант: `resolved_at` устанавливается один раз — при первом переходе в `STATUS_RESOLVED`.  
  Свойство `rating` оптимизировано: при наличии аннотации `_rating` — не делает запрос к БД.
- **`Vote`**  
  Уникальность `(user, issue)`. `vote=0` реализуется как `delete()`. Голосовать могут только граждане.
- **`IssuePhoto`**  
  Валидация: расширения `jpg/jpeg/png`, размер ≤ 5 МБ на файл, максимум 5 файлов.

### Логический слой
- Все view-функции защищены `@login_required`. Роль проверяется явно в коде — без middleware.
- `create_issue`: приоритет — ручные координаты (из клика по карте), затем геокодирование адреса.
- `vote_issue`: идемпотентно. Возвращает `rating` и `user_vote` в JSON.
- `map_view` и `get_issues_geojson` используют единую логику фильтрации.

### Геокодирование (`issues/modules/geocoding.py`)
- Единственный внешний API: **Nominatim OpenStreetMap** (`nominatim.openstreetmap.org`).
- Соблюдение ToS:
  - Уникальный `User-Agent` с email администратора (`ss@yandex.ru`)
  - `email` в reverse-запросах
  - Задержка `0.5s` между reverse-вызовами
  - Кэширование результатов на 2 часа
  - `viewbox` + `bounded=1` для ХМАО
  - Таймауты до 8 секунд
- Фолбэк: при промахе — возврат `"Ханты-Мансийск, ХМАО"` или координат.
- Все запросы браузера идут **только во внутренние Django API**, а не напрямую в Nominatim.

### Представление
- Шаблоны: иерархия через `base.html`, частичные рендеры для AJAX.
- Фронтенд: `vanilla JS`, MapLibre GL, поддержка `X-Requested-With: XMLHttpRequest`.
- Безопасность: `escapejs` для данных в попапах, `CSRF`-токен в `fetch`.

### Инфраструктура
- `docker compose` поднимает три сервиса: `db` (PostGIS 16 + 3.4), `web` (Django + Gunicorn) и `nginx` (отдаёт `/static/` и `/media/`, проксирует backend).
- База данных не пробрасывается наружу — доступ только через `docker compose exec db psql`.
- Кэширование по умолчанию: `LocMemCache` (можно заменить на Redis без изменений кода).
- Логирование уходит в STDOUT (`docker compose logs -f web`), формат единый для всех логгеров.  
- `/health/` — технический эндпоинт для проверок балансировщика и healthcheck контейнера.
- Безопасность: строгие валидаторы паролей, прод-флаги `SECURE_*`, настраиваемые `ALLOWED_HOSTS` и `CSRF_TRUSTED_ORIGINS`.

---

## Запуск

### Production-ready сценарий (сервер `pitching`, IP `51.250.28.139`)

1. **Подготовьте ветку и конфиг**
   ```bash
   git checkout feature/deploy-prep
   cp .env.example .env
   ```
   В `.env` заполните `SECRET_KEY`, `DB_NAME/USER/PASSWORD`, добавьте `pitching` и `51.250.28.139` в `ALLOWED_HOSTS`, пропишите реальные origin'ы в `CSRF_TRUSTED_ORIGINS`.  
   При необходимости включите `RUN_MIGRATIONS_ON_START=True`.

2. **Соберите и запустите стек**
   ```bash
   docker compose up -d --build
   ```
   Контейнер `web` собирает статику и (опционально) прогоняет миграции перед запуском Gunicorn.

3. **Примените миграции, если авто-режим выключен**
   ```bash
   docker compose exec web python manage.py migrate
   ```

4. **Создайте суперпользователя / выполните сервисные команды**
   ```bash
   docker compose exec web python manage.py createsuperuser
   docker compose exec web python manage.py check --deploy
   ```

5. **Проверьте сервис**
   ```bash
   curl http://localhost/health/
   ```
   На сервере проверьте `http://51.250.28.139` (или доменное имя `pitching`).

6. **Логи и остановка**
   ```bash
   docker compose logs -f web nginx
   docker compose down         # оставляет тома
   docker compose down -v      # удаляет pg/static/media данные
   ```

### Часто используемые команды
- `docker compose exec web python manage.py collectstatic --noinput` — entrypoint делает это автоматически, команда нужна только для ручной проверки.
- `docker compose exec db psql -U $DB_USER -d $DB_NAME` — доступ к PostGIS изнутри контейнера.
- `docker compose exec web python manage.py shell` — выполнение одноразовых скриптов.

---

## Руководство пользователя

### Для граждан

#### Регистрация
1. На главной странице в правом верхнем углу нажмите **«Регистрация»**.  
2. Заполните поля:
   - Имя*, Фамилия*  
   - Отчество (необязательно)  
   - Электронная почта*  
   - Телефон (необязательно)  
   - Роль* → выберите **«Гражданин»**  
   - Пароль* → не менее 12 символов (должен содержать буквы разного регистра, цифры и спецсимволы)  
   - Подтверждение пароля*  
3. Нажмите **«Создать аккаунт»**.  
4. Проверьте почту и перейдите по ссылке подтверждения.  
5. После подтверждения войдите в систему через **«Вход»**.

#### Создание обращения
1. Перейдите на **«Карту»** (`/issues/map/`).  
2. Укажите место проблемы:
   - Введите адрес в поле поиска (подсказки появятся автоматически), **или**  
   - Кликните по карте — появится синяя метка и кнопка **«Сообщить о проблеме здесь»** в правом верхнем углу.  
3. Нажмите эту кнопку.  
4. В открывшейся форме укажите:
   - Заголовок*  
   - Описание*  
   - Категория* (например, «Дороги», «Освещение»)  
   - Фотографии (до 5 файлов, JPG/PNG, до 5 МБ каждый)  
5. Нажмите **«Отправить»**.  
6. Обращение получит статус **«Открыто»**, затем **«В работе»** (когда его возьмёт должностное лицо), и, наконец, **«Решено»**.

#### Взаимодействие с обращениями
- **Голосование**: под деталями обращения — кнопки 👍 / 👎.  
  Голос можно отменить повторным нажатием.  
- **Комментирование**: внизу карточки — форма для комментария (доступна всем авторизованным).  
- **Фильтрация**: на карте — панель фильтров (категория, статус, поиск, сортировка).

---

### Для должностных лиц

#### Вход и управление
- Роль **«Официальное лицо»** назначается при регистрации (или администратором в `/admin/`).  
- После входа доступны:
  - Изменение статуса обращения («В работе» → «Решено»)  
  - Удаление обращений (только своих или с подтверждением)  
- Все действия логируются через Django messages.

---

## Внешние сервисы и ограничения

Проект использует **публичный Nominatim OpenStreetMap** (`nominatim.openstreetmap.org`) через проксирующие Django-представления:
- `/issues/api/geocode/`  
- `/issues/api/reverse-geocode/`  
- `/issues/api/search-address/`

Соблюдены все условия [Acceptable Use Policy](https://operations.osmfoundation.org/policies/nominatim/):
- Запросы инициируются только конечным пользователем (поиск/клик),  
- Реализовано кэширование,  
- Указан корректный `User-Agent` с email администратора,  
- Запрещены периодические/автоматические запросы.

> **Важно**: при росте нагрузки рекомендуется развёртывание локального Photon или использование коммерческого геокодера.

---

## Структура проекта

```
Map_of_local_issues/
├── home_page/              # Приложение для главной страницы и "О сайте"
│   ├── migrations/         # Миграции для home_page
│   │   └── __init__.py
│   ├── __init__.py
│   ├── apps.py             # Конфигурация приложения
│   ├── urls.py             # URL-маршруты для home_page
│   └── views.py            # Вьюхи: home_view, about_site
│
├── issues/                 # Основное приложение для работы с обращениями
│   ├── migrations/         # Миграции для issues
│   │   └── __init__.py
│   ├── modules/            # Вспомогательные модули
│   │   ├── __init__.py
│   │   └── geocoding.py    # Функции геокодирования: search_address, geocode_address, reverse_geocode
│   ├── __init__.py
│   ├── admin.py            # Регистрация Issue, Vote, Comment, IssuePhoto в админке
│   ├── apps.py             # Конфигурация приложения
│   ├── constants.py        # Константы: ISSUE_CATEGORY_CHOICES, ISSUE_CATEGORIES
│   ├── forms.py            # Формы: CommentForm (для комментариев)
│   ├── models.py           # Модели: Issue, Vote, Comment, IssuePhoto, Category
│   ├── tests.py            # Тесты (если есть)
│   ├── urls.py             # URL-маршруты для issues (namespace='issues')
│   └── views.py            # Вьюхи: map_view, create_issue, issue_detail, vote_issue, delete_issue, update_issue_status, API-эндпоинты
│
├── users/                  # Приложение для управления пользователями
│   ├── migrations/         # Миграции для users
│   │   └── __init__.py
│   ├── __init__.py
│   ├── admin.py            # Регистрация CustomUser в админке
│   ├── apps.py             # Конфигурация приложения
│   ├── forms.py            # Формы: CustomUserCreationForm, CustomAuthenticationForm, CustomSetPasswordForm
│   ├── models.py           # Модель: CustomUser, CustomUserManager
│   ├── tests.py            # Тесты (если есть)
│   ├── urls.py             # URL-маршруты для users
│   └── views.py            # Вьюхи: register_view, login_view, logout_view, verify_email_view, profile_view
│
├── static/                 # Статические файлы (CSS, JS, изображения)
│   ├── css/                # Стили
│   ├── icons/              # Иконки
│   ├── images/             # Изображения
│   └── js/                 # JavaScript (карта, фильтрация, AJAX)
│
├── templates/              # Шаблоны Django
│   ├── emails/             # Шаблоны писем (email_verification_body.txt)
│   ├── home_page/          # Шаблоны для home_page
│   │   └── home.html       # Главная страница
│   ├── issues/             # Шаблоны для issues
│   │   ├── create_issue.html  # Форма создания обращения
│   │   ├── issue_detail.html  # Детали обращения
│   │   ├── map.html        # Карта со всеми обращениями
│   │   └── partials/       # Частичные шаблоны
│   │       └── issues_list.html  # Список обращений (для AJAX)
│   ├── users/              # Шаблоны для users
│   │   ├── login.html      # Форма входа
│   │   ├── register.html   # Форма регистрации
│   │   └── profile.html    # Профиль пользователя
│   ├── about_site.html     # Страница "О сайте"
│   └── base.html           # Базовый шаблон
│
├── media/                  # Загруженные пользователем файлы (фото проблем, фото профиля)
│
├── Map_of_local_issues/    # Корневой модуль проекта
│   ├── __init__.py
│   ├── asgi.py             # ASGI-конфигурация
│   ├── context_processors.py  # Контекстные процессоры 
│   ├── settings.py         # Настройки проекта (PostGIS, кэширование, логирование, безопасность)
│   ├── urls.py             # Корневая маршрутизация (включает home_page, users, issues)
│   └── wsgi.py             # WSGI-конфигурация
│
├── .env                    # Переменные окружения (не в репозитории)
├── .env.example            # Пример переменных окружения
├── .gitignore              # Файлы, игнорируемые Git
├── docker-compose.yml      # Конфигурация Docker Compose (web, db)
├── Dockerfile              # Dockerfile для Django-приложения
├── error.log               # Лог ошибок 
├── manage.py               # Утилита управления проектом
├── README.md               # Документация проекта
└── requirements.txt        # Зависимости Python
```

---

Проект полностью готов к эксплуатации. Архитектура минимизирует риски, обеспечивает расширяемость и соответствует внешним ограничениям.